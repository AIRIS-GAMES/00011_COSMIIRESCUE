# 全体レビュー — 2026-09-13

Chromeの独立したテスト環境で確認。以下の指摘はレビュー時点の記録で、6件とも修正済み。

## 修正結果 — 2026-09-13

- 帰還時に全得点と人数を同期確定・保存。後続のアニメーションを中断しても失われない。
- 背景スクロールによる強制押し戻しを解除。左入力で戻れる。
- ナイフ等は回転楕円に沿って押し出し、短軸接触での大きな位置飛びを解消。
- 起動画像の読み込み失敗はロゴ画面にLOAD FAILEDとRETRYを表示。
- ミュートをモード共通の設定として保存。既存スコアは維持。
- BFCacheの復元では既存の計測ランを維持し、二重開始しない。
- バックグラウンドではBGM・効果音・ゲーム時間を停止。復帰だけでは再生せずRESUMEで再開する。通常曲と神楽の光の両方で確認。

検証: 自動テスト50件成功。`scripts/verify-review-fixes.mjs`で保存、ミュート再読み込み、音声停止と明示再開、ページ復元イベント、画像失敗とRETRYを確認。実機のアプリ切り替えとBFCache遷移は未確認。

## 指摘（優先度順）

### P1: チェックポイント帰還済みの得点が、演出中の離脱で失われる

- 対象: src/engine.js:43、src/engine.js:51、src/main.js:35。
- 20匹帰還で予定額10,275点。帰還開始から180ms後にPAUSE → TITLEを押すと、2匹・825点のみ保存され、残り18匹分が消えることを再現。
- beginReturnは仲間を全てcarryから外す一方、得点はbankOneで0.13秒ごとに加算する。homeNowは未処理のbankQueueを確定せずresetする。finishには全件精算があるが、TITLEや再読み込みにはない。
- 推奨: チェックポイント通過時に帰還分の全額・人数を確定して保存し、1匹ずつの演出は表示だけに分離する。単にTITLEで精算するだけでは、ページを閉じた場合を解決できない。

### P2: 左入力を続けても、見えない境界に押されて右へ移動する

- 対象: src/engine.js:73、src/engine.js:85。
- 障害物なしで左入力を10秒継続。内部vxは-225なのに、xは205から1,125へ増加。
- scrollXが常に毎秒110進み、プレイヤーxをscrollX+25以上に固定するため。左に見えている仲間や、通り過ぎたHOMEへ戻ろうとしても届かない状況になる。
- 推奨: 常時流れる背景とプレイヤーの世界座標を分離する。強制前進を残す設計なら、左入力の役割と戻れない境界を明示し、HOMEヒントも到達可能な地点に絞る。

### P2: ナイフ等の当たり判定と押し出し形状が一致しない

- 対象: src/engine.js:10、src/engine.js:110。
- 衝突は回転楕円で判定するが、接触後は半径rの円として押し出す。
- r=144、ry=60のナイフの短軸側に約1px食い込む配置で、1更新で約86ワールドpx押し出されることを再現。
- 推奨: 衝突と同じ回転楕円に沿う最小押し出しを使用する。長いナイフで特に目立ち、急なワープや追加衝突につながる。

### P2: 画像読み込み失敗時に、起動ロゴのLOADINGから進めない

- 対象: src/main.js:99、index.html:9。
- green.pngの読み込みを失敗させて再現。内部にはLOAD FAILEDが出るが、その上にboot-screenが残り、ユーザーにはLOADINGしか見えない。
- 推奨: エラー時に起動ロゴ側へ失敗とRETRYを表示するか、boot-screenを閉じて復旧操作を見せる。

### P2: HARDで設定したミュートが再読み込みで解除される

- 対象: src/main.js:10、src/main.js:28、src/main.js:87、src/storage.js:4。
- HARD選択 → ミュート → 再読み込みでaudio.enabledがfalseからtrueへ戻ることを再現。
- 設定保存先が選択中のモード別ストレージなのに、起動時はNORMAL側しか読み込まない。
- 推奨: サウンド設定は共通保存し、スコアのみNORMAL/HARDに分離する。

### P2: ブラウザのページ復元後、そのプレイの計測が止まる

- 対象: src/main.js:91、src/analytics.js:34、src/analytics.js:46。
- pagehideでanalytics.activeをfalseにするが、pageshowで復帰する処理がない。persisted=trueのpagehide/pageshowを模擬すると、activeがfalseのままになる。
- BFCacheから既存ゲーム画面へ戻ってRESUMEした場合、以後の帰還・レインボー・終了イベントが捨てられる。通常の新規PLAYでは復帰する。
- 推奨: persistedな離脱と、本当のラン終了を区別する。復元時に既存runを継続し、Startや終了集計の二重送信を防ぐ。
- 実ブラウザのBFCache遷移そのものではなく、対応ライフサイクルイベントで再現した指摘。

## 確認できた機能

- 自動テスト45件: 全件成功。
- 縦向き起動から横レイアウトへの自動移行、回転後の操作、画面遷移: 成功。
- 20匹を一度に帰還させた場合のレインボー発動、360度ビームの上下左右の破壊、12秒終了: 成功。
- 神楽の光.mp3への切り替え、一時停止中のBGMと時間停止、通常BGMへの復帰: 成功。
- GameAnalytics公式SDKによるイベント生成、モード別属性、帰還イベントの重複防止: 通信を遮断・模擬した環境で成功。本番にテストプレイデータは送っていない。

## ゲームデザイン上の検討事項

- HOME BONUSは非表示の救出コンボ倍率も含むため、人数から得点を直感的に予測しにくい。バグとは分けて、倍率を簡素化するか検討したい。
- HARDの強障害物直撃即死と20匹同時帰還の組み合わせについて、自然なプレイでの発動頻度は未評価。今回のレインボー確認では仲間をテスト用に配置しているため、到達しやすさの証明ではない。
- Chromeのスマホ相当表示で確認。実機iPhone/Safariでの音声・複数指操作・発熱・長時間フレームレートは未確認。

再現: `node scripts/review-repro.mjs`。既存ユーザーの保存データは変更しない。
