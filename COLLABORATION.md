# おっ！サン期間限定コラボ

## 現在の設定

設定は **`src/collaboration-config.js` の1か所**。
開催日時は未定のため、現在は `enabled:true`、開始・終了日時は `null`。

```js
export const collaboration={
  enabled:true,
  startsAt:null,
  endsAt:null,
  target:5,
  feverSeconds:10,
  spawnRate:.30,
  feverSpawnRate:.80,
};
```

## 遊び方

- 通常の仲間に混ざっておっ！サンが約30%で出現する。主人公はコスミーのまま。
- おっ！サンをHOMEへ累計5体届けると、10秒間の「おっ！サンフィーバータイム」。
- 接触して仲間にした段階ではカウントしない。HOMEへの帰還が確定した時点で加算。
  衝突で離れたおっ！サンは見た目・種類を保持し、再び救出できる。
- フィーバー中は従来のレインボーと同じ無敵・前方ビームによる障害物破壊。
  新たな仲間のおっ！サン出現率が80%に上がり、0.8秒ごとに近くへ追加出現する
  （追加分は画面周辺に最大8体）。
- 5体ごとに再発動可能。端数は持ち越す。例えば7体届けた場合、発動後の進捗は2/5。
  大量帰還・フィーバー中の再達成でも残り時間は最大10秒に戻り、無制限に延長しない。
- コラボ中は通常の20体レインボー条件をフィーバー条件に置き換える。OFFなら従来条件に戻る。
- コラボ中は血付きのナイフ・フォークを出現候補から外し、被弾時の流血演出も停止する。
  ダメージ・仲間の離脱判定は維持。コラボOFF時は元の障害物・流血演出に戻る。
- タイトルのコスミー全員を維持し、おっ！サンを追加表示する。
  コラボ案内ボタンを押すと内容説明が開く。閉じるボタン・Escapeでタイトルへ戻る。
- プレイ上部は**いま連れているおっ！サンの数**を表示する（通常のコスミーは含まない）。
  HOME帰還後は0体へ戻る。フィーバー発動用の累計帰還数は別に管理し、5体条件を維持。
  フィーバー中も手持ち数を表示し、あわせて残り秒数を表示する。
  発動時に1.5秒の案内、結果に今回の救出数を表示。
- カウントは1プレイ内のみ。リトライでリセット。保存形式・既存スコア・得点計算式は変更しない。
- 効果音・BGMは既存のファイル再生を再利用。通常曲とフィーバー曲は同時再生しない。
  一時停止・バックグラウンドでは時間と音声が止まり、RESUMEで継続する。

## 画像・セリフのルール

- `Asset/collaborations/ohsun/characters/ohsun_07.png`〜`ohsun_13.png` をそのまま使用。
- 原本ファイルに変更なし。背景除去、切り抜き、色変更、反転、パーツ分割、描き足しなし。
- 描画は元の縦横比を保つ縮小と位置移動のみ。通常キャラの回転・変形・透過処理は適用しない。
- 画像内の権利表記を含めた全体を表示し、画面にも `© SUN-TV` を表示する。
- セリフ・吹き出し・キャラクター音声は追加しない。テキストはゲーム側の案内と数値のみ。
- コラボ画像を画面遷移の顔アニメーションや障害物の破片演出に使わない。
- この作業ではローカル確認とCapacitor同期まで。外部への公開・配布は行っていない。

## 開催日時を設定する

日時にはタイムゾーンを必ず付ける。例（実際の開催日時ではない）:

```js
startsAt:'2026-10-01T00:00:00+09:00',
endsAt:'2026-11-01T00:00:00+09:00',
```

開始日時を含み、終了日時は含まない。未指定側は制限なし。
不正な日時・開始以降でない終了日時では無効になる。
判定は端末日時に基づく。プレイ中に終了時刻を迎えた場合、そのプレイは継続し、
次のタイトル表示・ゲーム開始から通常版へ戻る。タイトル待機中は自動で表示を切り替える。
開始前のビルドには期間中に使用する素材を含め、終了後のビルドには含めない。

## コラボを終了して元に戻す

1. `src/collaboration-config.js` の **`enabled:false`** に変更する。
2. 次を実行する。

```sh
npm test
npm run build
npx cap sync ios
```

3. MacのXcodeでiOSビルド・実機確認後、通常版更新を配布する。

これでコラボ表示、出現、進捗、フィーバーがOFFになり、通常のタイトル・仲間・
20体を一度に帰還させるレインボーに復帰する。既存の記録・音声OFF設定は保持。
ビルドは **`dist/Asset/collaborations` を含めず**、同期後の
**`ios/App/App/public/Asset/collaborations` からも素材が消える**。
リポジトリ内の原本は残す。生成物の直接編集や、過去の `remove-collab.py` は不要。

日付による自動OFFは、すでにインストールされた素材自体を削除するものではない。
端末から素材を除外するには終了版アプリへの更新が必要。

### 元に戻す動作だけを試す

設定ファイルを変更せず、OFF版を一度だけ生成する確認用オプション:

```sh
npm run build -- --without-collaboration
npx cap sync ios
node scripts/verify-collaboration-off.mjs
```

その後 `npm run build` と `npx cap sync ios` を実行すると、ソース設定どおりの版に戻る。
ローカルサーバーの `/` はソース設定、`/dist/index.html` は生成済み版を表示する。

## 実装箇所

- `src/collaboration-config.js`: 有効フラグ・期間・数値設定。
- `src/collaboration.js`: 期間判定、種類の割り当て、帰還カウント、フィーバー。
- `src/collaboration-ui.js`: 条件付きのタイトル／結果／権利表記。
- `src/engine.js`: 仲間の種類保持、帰還・ダメージ・更新処理との接続。
- `src/renderer.js`, `src/style.css`: 原本の描画、進捗、限定画面。
- `src/main.js`, `src/rainbow.js`, `src/incoming.js`: 既存音声・ビーム・障害物との接続。
- `scripts/build.mjs`: ON/OFFに応じた素材の収録と生成設定。
- `scripts/audit-unused-assets.mjs`: 使用素材の登録。

## 検証

- `npm test`: 64件（新規コラボ9件を含む）。
- `npm start` 起動中に `node scripts/verify-collaboration.mjs`:
  タイトルの全コスミー表示、説明ボタンの開閉・キーボード操作、手持ち数の増減と帰還後0体表示。
  3+2体の帰還、発動、曲切替、時間停止、バックグラウンド、復帰、終了、結果、日時終了。
  Canvasの実描画も監視し、画像全体・不透明・フィルターなし・回転なし・縦横比維持を確認。
- `node scripts/verify-collaboration-off.mjs`: OFF版の素材除外、リクエストなし、通常タイトル、20体レインボー復帰。
- 既存のBGM／WAV／通常レインボー／バックグラウンド回帰テスト。
  `verify-rainbow.mjs` と `verify-review-fixes.mjs` は通常仕様の検証としてコラボOFF設定を使用。
- `test-results/collaboration-*.png`: タイトル・フィーバー・結果・縦向き・通常版の確認画像。
- PNG原本のSHA-256が作業前と一致。配布画像も原本と一致。
- ON版56アセット／OFF版49アセットをビルドし、両方のCapacitor同期を確認。

Windowsのため、iOSネイティブビルド・iPhoneでの実プレイ／画面録画は未実施。
